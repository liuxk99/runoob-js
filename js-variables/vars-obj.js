var stooge = {
    // stooge 是一个对象
    frist_name: "Julie",    // 等号左为属性名，右侧为属性值
    last_name: "beck"    // 属性名如果是合法的标识符，可省略引号
};
// "frist-name", last_name, "Julie", "beck" 都是对象字面量

console.log(stooge);